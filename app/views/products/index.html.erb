<div class="jumbotron">
	<h1 class="text-center">Productos y categorias</h1>
</div>
<table>
  <tr>
    <th>Producto</th>
    <th>Categoría</th>
  </tr>

<% @products.each do |p| %>
  <tr>
	<td><%= p.name  %> </td>
	<td><%= p.category.name %>	</td>
	 </tr>
<% end  %>
</table>



<form method="POST" action="<%= products_index_path %>">
	<%= hidden_field_tag :authenticity_token, form_authenticity_token %>
	<input type="text" name="product_name" placeholder="Ingrese nombre:">
	<input type="number" name="product_cost" placeholder="Ingrese precio">
	<input type="submit" name="Buscar">
</form>

